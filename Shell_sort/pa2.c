#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "shell_array.h"
#include "shell_list.h"
#include "sequence.h"

int main(){
    int *seq_size;
    int length = 100;
    /*
    long *sequence = (long *)malloc(100*sizeof(long));

    *sequence = Generate_2p3q_Seq(length,*seq_size);
    
    printf("size of the sequence is: %d", *seq_size);

    printf("\n complete \n");
    */
    test(19);
    return 0;
}